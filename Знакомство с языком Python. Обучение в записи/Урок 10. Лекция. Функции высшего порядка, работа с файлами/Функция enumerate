
                                                        Функция enumerate

Функция enumerate() применяется к итерируемому объекту и возвращает новый итератор с кортежами из индекса и элементов входных данных. Функция enumerate() позволяет пронумеровать
набор данных.
enumerate (['Казань', 'Смоленск', 'Рыбки', 'Чикаго']) - вызываем фунцию передаем список и значения городов Казань, Смоленск, Рыбки, Чикаго
[(0, 'Казань'), (1, 'Смоленс'), (2, 'Рыбки'), (3, 'Чикаго')] - на выходе получаем список в котором у нас каждый элемент будит иметь тип даных кортеж. Где в кортежи первый элемент
                                                               это индекс, а второй наше значения

Функция enumerate() позволяет пронумеровать набор данных.
users = ['user1', 'user2', 'user3']
data = list(enumerate(users))
print(data) # [(0, 'user1'), (1, 'user2'), (2, 'user3))]